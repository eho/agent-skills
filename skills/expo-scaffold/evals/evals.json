{
  "skill_name": "expo-scaffold",
  "evals": [
    {
      "id": 1,
      "prompt": "Create a new Expo React Native app called FieldNotes using Expo SDK 55. Set up NativeWind, gluestack-ui v3 with basic components, a placeholder screen, EAS Build, and EAS Update.",
      "expected_output": "A scaffolded Expo SDK 55 app using src/app Expo Router layout, expo-dev-client, stable compatible NativeWind, official gluestack setup delegated through expo-gluestack-setup with a Gluestack Handoff, a starter screen using verified gluestack components, EAS build profiles, EAS update channels, docs updates, cleanup notes, and verification notes.",
      "files": [
        "src/app/_layout.tsx",
        "src/app/index.tsx",
        "src/global.css",
        "metro.config.js",
        "tailwind.config.js",
        "eas.json",
        "package.json"
      ],
      "expectations": [
        "The generated Babel config includes both the Expo preset configured with jsxImportSource nativewind and the nativewind/babel preset for the stable NativeWind v4 path.",
        "The app config preserves existing web settings while setting web.bundler to metro when NativeWind v4 requires it.",
        "EAS build and update package scripts work on a clean checkout by using a local eas-cli dev dependency or an explicitly verified package-manager invocation.",
        "For SDK 55 and later, EAS update scripts include explicit --environment values matching their preview and production channels."
      ]
    },
    {
      "id": 2,
      "prompt": "Scaffold an Expo Router app named DispatchDesk using Bun. Make it iOS and Android, add gluestack-ui v3, NativeWind, preview and production EAS updates, and scripts for development, preview, and production builds.",
      "expected_output": "A Bun-friendly Expo SDK 55 Router project with src/app routes, expo-dev-client, NativeWind and official gluestack configured through the expo-gluestack-setup handoff, exact gluestack package versions when using the v3 manual path, package-local Bun workspace links preserved when needed, EAS profiles/channels, package scripts, and no invented credentials or app store IDs.",
      "files": [
        "src/app/_layout.tsx",
        "src/app/index.tsx",
        "src/global.css",
        "package.json",
        "bun.lock",
        "eas.json"
      ],
      "expectations": [
        "The scaffold uses Bun consistently without creating npm, Yarn, or pnpm lockfiles in the final project.",
        "EAS scripts do not assume a globally installed eas binary; they use a local eas-cli dependency or a verified Bun one-off command.",
        "The preview and production EAS update scripts include matching --environment preview and --environment production flags for SDK 55 and later.",
        "Expo-generated agent context is merged into the repository AGENTS.md if create-expo-app generated agent files."
      ]
    },
    {
      "id": 3,
      "prompt": "Create a production-oriented Expo starter with gluestack. If the latest NativeWind version is not clearly stable for the latest Expo SDK, ask me before choosing it.",
      "expected_output": "The agent verifies NativeWind stability and asks before using a preview/pre-release line instead of silently installing it. If using the v4 path, package.json pins nativewind to the verified v4 major and Tailwind CSS 3.4.x.",
      "files": [
        "package.json",
        "tailwind.config.js",
        "metro.config.js",
        "babel.config.js"
      ],
      "expectations": [
        "If the stable NativeWind v4 path is selected, package.json pins nativewind to a v4 major and Tailwind CSS to the 3.4 line.",
        "The v4 Babel config includes nativewind/babel and keeps react-native-reanimated/plugin last.",
        "If NativeWind v5 is selected, the output explicitly labels it as pre-release/preview and follows the v5 docs instead of reusing v4 snippets.",
        "If official Expo docs show a newer stable SDK has replaced SDK 55, the agent reports that before continuing with the older default."
      ]
    },
    {
      "id": 4,
      "prompt": "Create an Expo app starter that may later use Expo API routes and a landing website. Ask me what structure I want before scaffolding, then choose a clean monorepo if I request backend or landing support.",
      "expected_output": "The agent asks about mobile-only, backend/API support, landing-site support, sticky app identifiers, and landing framework/SEO needs, then scaffolds or proposes an apps/mobile/src/app, optional apps/landing with correctly grouped Vite dependencies, and packages/api/shared workspace structure.",
      "files": [
        "package.json",
        "apps/mobile/src/app/_layout.tsx",
        "apps/mobile/src/app/index.tsx",
        "apps/mobile/eas.json"
      ],
      "expectations": [
        "The agent asks whether API routes means Expo Router API routes inside the Expo app or a separate backend/API package.",
        "If Expo API routes are requested, route adapter files are created under apps/mobile/src/app/api or the selected Expo route root, not only in packages/api.",
        "If only future backend support is requested, packages/api is documented as future backend support and not claimed as wired Expo API routes."
      ]
    },
    {
      "id": 5,
      "prompt": "Scaffold an Expo Router app with gluestack-ui v3 that follows system light/dark mode, has a native splash screen with light and dark assets, and includes an optional animated launch overlay.",
      "expected_output": "A scaffolded Expo app that configures gluestack provider mode/system theme through tokenized light/dark CSS variables, avoids broad dark: class duplication, configures expo-splash-screen through the config plugin, and implements any animated launch as a React/Reanimated overlay after the native splash hides.",
      "files": [
        "src/app/_layout.tsx",
        "src/components/layout/AppLaunchOverlay.tsx",
        "src/lib/theme-runtime.ts",
        "app.json"
      ],
      "expectations": [
        "The root layout imports global.css exactly once and wraps Expo Router with the official GluestackUIProvider from the handoff.",
        "The starter screen uses gluestack token classes rather than broad duplicated dark: class pairs.",
        "Native splash configuration uses the expo-splash-screen config plugin rather than legacy top-level expo.splash configuration for a new scaffold."
      ]
    },
    {
      "id": 6,
      "prompt": "Scaffold an Expo SDK 55 app with gluestack-ui v3 using Bun. Do not use the gluestack CLI unless absolutely necessary; previous runs hung at the Bun package-manager prompt. Do not use unofficial gluestack replacements unless I explicitly approve them.",
      "expected_output": "The scaffold delegates gluestack work to expo-gluestack-setup or follows that skill's workflow. The gluestack handoff records official manual setup as the default for v3, exact package metadata, copied official provider/component source, provider wiring, and outcome `manual_installed` after verification. It does not create unofficial replacement primitives. It uses CLI only if current official docs/source no longer provide enough manual setup detail, with timeout/capture/cancel behavior if a CLI command hangs.",
      "files": []
    },
    {
      "id": 7,
      "prompt": "Review and finish verification for a Bun workspace Expo scaffold. `expo-doctor` reports duplicate native modules, but the duplicate entries are the same versions under Bun workspace link paths. Verification also generated `.expo/`, `apps/landing/dist/`, and temporary EAS inspect output. Clean up what should not be committed and report any remaining risk.",
      "expected_output": "The agent distinguishes duplicate native module versions from duplicate same-version entries caused by Bun workspace links, documents the same-version warning as residual risk, recommends a native development or preview build before production readiness, preserves required Bun package-local link folders, removes generated `.expo`, `dist`, and temporary EAS inspect output, updates or checks `.gitignore`, and reports cleanup plus verification results.",
      "files": [
        ".gitignore",
        "bun.lock",
        "apps/mobile/package.json",
        "apps/landing/package.json"
      ]
    },
    {
      "id": 8,
      "prompt": "In my existing Expo app, only repair gluestack-ui setup. Do not scaffold a new project.",
      "expected_output": "The scaffold skill should not take over the request. The correct routing is to use expo-gluestack-setup for existing-app gluestack-only repair, avoiding create-expo-app, EAS setup, project-structure decisions, or broad starter changes.",
      "files": [
        "package.json",
        "src/components/ui",
        "src/app/_layout.tsx"
      ]
    },
    {
      "id": 9,
      "prompt": "Scaffold an Expo starter with gluestack. During setup, the gluestack handoff returns `interactive_cli_required` because official manual source cannot be verified.",
      "expected_output": "The scaffold stops before theme wiring, placeholder screen wiring, full verification, or final success language. It reports the gluestack diagnostics and exact manual command from the handoff, and resumes only after the user completes the interactive gluestack step.",
      "files": [
        "src/app/_layout.tsx",
        "src/app/index.tsx",
        "src/components/ui"
      ],
      "expectations": [
        "The scaffold does not wire placeholder screens or report completion while the gluestack handoff outcome is interactive_cli_required.",
        "The pause report includes the exact gluestack command for the user to run and diagnostics from attempted automated setup."
      ]
    },
    {
      "id": 10,
      "prompt": "Use my existing project-bootstrap AGENTS.md as the repo rules source of truth, then scaffold a new Expo SDK 55 app. If create-expo-app generates Expo agent files, merge the useful Expo context into AGENTS.md instead of replacing it.",
      "expected_output": "The agent preserves the existing project-bootstrap AGENTS.md structure and merges only durable Expo-specific context such as SDK docs, package-manager commands, expo install guidance, Expo Router route root, CNG/native-directory notes, dev-client workflow, and EAS boundaries. Generated CLAUDE.md and .claude/settings.json are removed unless explicitly requested.",
      "files": [
        "AGENTS.md",
        "apps/mobile/AGENTS.md",
        "CLAUDE.md",
        ".claude/settings.json"
      ],
      "expectations": [
        "The existing AGENTS.md is not overwritten wholesale by create-expo-app output.",
        "AGENTS.md includes concise Expo-specific runtime and command guidance merged with the existing project-bootstrap package-manager, docs, testing, and safety sections.",
        "Generated CLAUDE.md and .claude/settings.json are removed or explicitly justified if kept."
      ]
    },
    {
      "id": 11,
      "prompt": "Scaffold an Expo SDK 55 app with NativeWind v4 and gluestack-ui v3. Use GluestackUIProvider mode=\"system\" and make sure the app does not crash with NativeWind's `Unable to manually set color scheme without using darkMode: class` error.",
      "expected_output": "The scaffold creates the Metro input global.css with @tailwind base/components/utilities, uses withNativeWind with the real CSS path, sets static darkMode: \"class\" in tailwind.config.js without process.env.DARK_MODE branching, imports global.css before rendering GluestackUIProvider, and includes a verification step that bundles or otherwise exercises NativeWind CSS processing.",
      "files": [
        "src/global.css",
        "metro.config.js",
        "tailwind.config.js",
        "src/app/_layout.tsx"
      ],
      "expectations": [
        "global.css contains @tailwind base, @tailwind components, and @tailwind utilities.",
        "tailwind.config.js has static darkMode: \"class\" and no process.env.DARK_MODE fallback.",
        "metro.config.js points withNativeWind at the same global.css path imported by the root layout.",
        "The root layout imports global.css before rendering GluestackUIProvider mode=\"system\"."
      ]
    }
  ]
}
