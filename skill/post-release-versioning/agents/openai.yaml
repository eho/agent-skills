interface:
  display_name: "Post-Release Versioning"
  short_description: "Tag releases and prepare next versions"
  default_prompt: "Use $post-release-versioning to tag the completed release and prepare the next version."
policy:
  allow_implicit_invocation: false
